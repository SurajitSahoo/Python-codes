# Sample list of words
words = ["apple", "banana", "cherry", "date", "elderberry"]

# Using list comprehension to extract the first character of each word
first_characters = [word[0] for word in words]

# Output the list of first characters
print(first_characters)
